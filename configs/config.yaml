defaults:
  - _self_
  - model: base
  - dataset: base

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}

wandb:
  project: "stat-ood"
  entity: null  # Set to your username if needed
  name: "${now:%Y-%m-%d_%H-%M-%S}"

experiment:
  seed: 42
  debug: false
  device: "auto" # Auto-detects (CUDA > MPS > CPU)

ood_method: "mahalanobis" # or 'energy'
